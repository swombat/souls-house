# Local Dev Login

This is a login for the development server, not a list of credential keys —
for those, see `config/credentials/production.example.yml`.

- **Email**: daniel@granttree.co.uk
- **Password**: password

Used for testing the development server at `http://localhost:3100`
