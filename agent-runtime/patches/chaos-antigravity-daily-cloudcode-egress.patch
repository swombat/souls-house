diff --git a/sys/modules/clamp/src/egress.rs b/sys/modules/clamp/src/egress.rs
--- a/sys/modules/clamp/src/egress.rs
+++ b/sys/modules/clamp/src/egress.rs
@@ -56,8 +56,11 @@ use crate::proxy::{RecordParts, TeeBody, WiretapExchange, WiretapSink, redact_he
 /// Hosts `agy` is known to need: the Cloud Code agent backend, the OAuth token
 /// endpoint, and the generative-language surface. Everything else the binary
 /// references (telemetry, Play, mTLS variants) is deliberately absent.
-pub const ANTIGRAVITY_ALLOWED_HOSTS: [&str; 3] = [
+pub const ANTIGRAVITY_ALLOWED_HOSTS: [&str; 6] = [
     "cloudcode-pa.googleapis.com",
+    "daily-cloudcode-pa.googleapis.com",
+    "www.googleapis.com",
+    "lh3.googleusercontent.com",
     "oauth2.googleapis.com",
     "generativelanguage.googleapis.com",
 ];
